package com.company;

public class pump extends Thread{
    //constructor getter setter to String.

    void takeFromPump(Float volume){
        //метод "take()" из класса Pump - забирает 'volume' литра топлива из бака  - каждый раз когда его вызывают
    }
    void put(Float volume){
       // метод "put()" толкает в двигатель 'volume' литра - каждый раз когда его вызывают
    }

    public void run(){
       // while(){
            //.....
        //    take();
        //каждый  take() - забирает 0.2 литра
        //    put();
       // каждый  put() - толкает в двигатель 0.15 литра

        //}
    }

}
